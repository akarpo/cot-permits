# Local working cache — regenerated from permits.json.gz by update_permits.py.
permits.db
permits.db-journal
permits.db-wal

# Data blob — hosted on R2, not committed to git.
permits.json.gz

# Raw scrape exports live in ~/Downloads, not in the repo
troy_permits_*.xlsx
*.xlsx

# Python
__pycache__/
*.pyc

.DS_Store

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
